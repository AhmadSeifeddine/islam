@props(['coloredTitle' => '', 'title' => '', 'subTitle' => ''])

<div class="text-center mb-10">
    <h2 class="display-2 fw-bold mb-4"> {{$title}} <span class="gold">{{$coloredTitle}}</span></h2>
    <p class="lead text-body-secondary fs-4 mb-0">{{$subTitle}}</p>
</div>
